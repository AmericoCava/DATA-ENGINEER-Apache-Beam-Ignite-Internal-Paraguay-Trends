from setuptools import find_packages
from setuptools import setup

setup(
    name='file',
    version='0.0.1',
    packages=find_packages(),
    install_requires=(),
    include_package_data=True,
    description='Cancel - Proceso PY - Ignite Portfolio Trends'
)